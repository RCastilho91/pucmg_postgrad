// creating surname variable
const surname = "Castilho";

// creating an addition function
const sillyFunction = (number, addition) => {
    return number + addition
}

// exporting content
module.exports = sillyFunction;